import React from 'react';

export const TablesSchemaContext = React.createContext(null);
export const GrandPublicContext = React.createContext(null);
export const AccesRestreintContext = React.createContext(null);
